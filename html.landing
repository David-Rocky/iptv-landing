<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>XTREAM IPTV - Premium Streaming</title>
<style>
    :root{
        --bg:#0d0d0d;
        --card:#111;
        --muted:#666;
        --accent1:#ff005a;
        --accent2:#6400ff;
        --card-2:#1a1a1a;
    }
    *{box-sizing:border-box}
    body { margin:0; padding:0; background:var(--bg); font-family:Arial, Helvetica, sans-serif; color:#eee; -webkit-font-smoothing:antialiased;}
    .container { max-width:700px; margin:30px auto; background:var(--card); border-radius:14px; overflow:hidden; box-shadow:0 8px 30px rgba(0,0,0,.6); }
    .header {
        background:linear-gradient(135deg,var(--accent1),var(--accent2));
        padding:36px 20px;
        text-align:center;
        color:white;
    }
    .header h1 { margin:0; font-size:34px; letter-spacing:1px; line-height:1.05; }
    .header p { margin-top:10px; font-size:15px; opacity:.95; }

    .free-badge {
        display:inline-block;
        margin-top:14px;
        background:rgba(255,255,255,0.12);
        color:white;
        padding:8px 14px;
        border-radius:999px;
        font-weight:700;
        font-size:13px;
        letter-spacing:.6px;
        text-transform:uppercase;
        border:1px solid rgba(255,255,255,0.12);
        text-decoration:none;
        transition:transform .14s ease, background .14s ease;
    }
    .free-badge:hover { transform:translateY(-2px); background:rgba(255,255,255,0.16); }

    .banner-img { width:100%; display:block; }

    .section { padding:24px 20px; color:#eee; }
    .section h2 { text-align:center; margin-bottom:18px; color:var(--accent1); }

    .features { display:flex; flex-wrap:wrap; gap:12px; justify-content:space-between; }
    .feature-box {
        flex:1 1 calc(50% - 12px);
        min-width:160px;
        background:var(--card-2);
        padding:14px;
        border-radius:10px;
        border:1px solid #222;
    }
    .feature-box h3 { margin:0 0 8px 0; font-size:15px; color:var(--accent1); }
    .feature-box p { margin:0; font-size:13px; opacity:.95; }

    .cta {
        text-align:center;
        padding:28px 20px;
        background:#0f0f0f;
    }
    .cta a.primary {
        background:var(--accent1);
        color:white;
        padding:14px 26px;
        text-decoration:none;
        border-radius:30px;
        font-weight:700;
        font-size:15px;
        display:inline-block;
        transition:transform .12s ease, opacity .12s ease;
    }
    .cta a.primary:hover { transform:translateY(-3px); opacity:.98; }

    .footer { text-align:center; padding:14px; font-size:12px; color:var(--muted); background:#0b0b0b; }

    @media (max-width:520px){
        .header h1{ font-size:26px; }
        .feature-box{ flex:1 1 100%; }
        .container{ margin:16px; }
    }
</style>
</head>
<body>
<div class="container">

    <div class="header">
        <h1>XTREAM IPTV</h1>
        <p>Unlimited Live TV • Movies • Series • Sports in HD/4K</p>
        <a class="free-badge" href="#" title="Start your free test">FREE TEST • 24H</a>
    </div>

    <img class="banner-img" src="https://via.placeholder.com/1200x400/000000/ffffff?text=IPTV+PROMO+BANNER" alt="IPTV Banner">

    <div class="section">
        <h2>🔥 Why Choose Our IPTV?</h2>

        <div class="features">
            <div class="feature-box">
                <h3>+18,000 Live Channels</h3>
                <p>All countries • Sports • News • Entertainment — access any channel in seconds.</p>
            </div>

            <div class="feature-box">
                <h3>+80,000 VOD</h3>
                <p>Latest movies & series updated daily with easy search and resume playback.</p>
            </div>

            <div class="feature-box">
                <h3>Ultra Stable Servers</h3>
                <p>Low latency, no freezing — powered by high-availability clustered servers.</p>
            </div>

            <div class="feature-box">
                <h3>Compatible With All Devices</h3>
                <p>Smart TV • Android • iOS • Firestick • MAG • Enigma • PC.</p>
            </div>
        </div>
    </div>

    <div class="cta">
        <a class="primary" href="#">GET YOUR FREE TEST</a>
    </div>

    <div class="footer">
        XTREAM IPTV © 2025 — Premium Streaming Experience
    </div>

</div>
</body>
</html>
